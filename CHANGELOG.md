# Changelog

All notable changes to this project are documented here. This project is
currently pre-1.0 (see `config.mk`'s `EXPANSION_VERSION_*` and
[`docs/public_api_policy.md`](docs/public_api_policy.md) for exactly what
that means for SemVer/compatibility).

The `## [Unreleased]` section below is rendered **deterministically** from
machine-readable fragments in [`changelog_fragments/`](changelog_fragments/) by
`python3 -m scripts.release_rehearsal.changelog render` (or `write` to update this
file in place); see [`docs/release_process.md`](docs/release_process.md).
Do not hand-edit the text between the two HTML comment markers below --
`make release-check` fails actionably if it drifts from the fragments.

## [Unreleased]

<!-- release-notes:unreleased:begin -->
### Added

- Add a live ARM debugging smoke target that launches the modern debug ROM under mGBA's GDB server and proves remote attach, register access, a symbolic AgbMain breakpoint, continue, and backtrace.
- Add a project-scoped Copilot development workflow skill for feature and bug triage, selective change gates, automated evidence, autonomous merge and closure, and explicitly authorized direct-master delivery.
- Add structured GitHub issue forms for feature review and bug triage, requiring dependency/conflict analysis, exact build configuration, reproduction evidence, expected versus actual behavior, and game screenshots.
- Extend the project development workflow with requirement dependency/conflict tracking, local decomp/FEBuilder/Nightmare/clean-ROM investigation resources, IDA-first and Ghidra cross-check guidance, and final tool/version evidence.
- Install and ARM-probe GDB in quickstart, and require debugger-backed evidence in the project development workflow when register, stack, symbol, memory, or control-flow state matters.
- Add four independent, default-off starter-feature build flags (EXPANSION_MECHANICS_HOOKS, EXPANSION_MECHANICS_SAMPLE, EXPANSION_DANGER_OVERLAY_MENU, EXPANSION_STARTER_CONTENT), the public battle-stat mechanics hook registry, the player-facing danger/range overlay menu surface, and the bundled generated-data content example they gate; a default build links none of them and stays byte-identical (see docs/starter_features.md). (#6)
- Add a typed ID space (include/id_space.h, generated from scripts/generated_data/idspace.py) with per-domain technical-max/configured-cap macros and compile-time range diagnostics, and raise the configurable item ID cap to 0xCE (the default build's cap is unchanged); no EXPANSION_SAVE_COMPAT_EPOCH change (see docs/id_space.md, reports/id_space_audit.md). (#10)
- Add production 32 MiB Japanese and Simplified Chinese support: 3,414/3,414 compressed game messages and all 143 raw surfaces per locale, with zero fallback, exclusion, unresolved record, or runtime leakage; live-ROM-bound Japanese raw provenance; complete system/talk CJK font coverage; full FE control-stream tokenization and bounded UTF-8 substitutions; persisted first-start/Config switching; and localization host/build/runtime gates with captured CJK playtests (see docs/localization.md, docs/game_locale_sources.md, and docs/cjk_fonts.md). (#18)
- Add a debug-only bounded chapter and skirmish selector backed by typed chapter/world-map metadata and a lifecycle-safe GameControl handoff. (#123)
- Add debug-only Flag/Chapter turn overrides and one-phase red/green CPU or BLOCKED controls, with safe lifecycle reset, phase-event ordering, and release omission. (#124)
- Add typed, read-only title/map/prep debugtools State and Engine diagnostics with bounded views and exact forced display restoration. (#127)
- Add the modern-debug-only typed volatile save-fixture API and built-in Save State flow: validated CURRENT game/suspend sources are sanitized into the existing 32 KiB EWRAM overlay, previewed and confirmed twice, continued once by GameControl, and protected by hard cartridge-write blocking without changing the save format or compatibility epoch. (#128)

### Changed

- Bump EXPANSION_SAVE_COMPAT_EPOCH and SAVE_FORMAT_VERSION_CURRENT from 1 to 2: struct ExpansionUserPrefs (include/expansion_save_prefs.h) now occupies part of struct ExpansionSaveMeta's reserved tail, and SAVE_COMPAT_MIGRATABLE_OLDER is added to enum SaveCompatState with a mechanical, no-wipe migration path (scripts/modernize/save_format_tool.py migrate; see docs/save_format.md, docs/migration_registry.md). (#18)

### Fixed

- Raise the combined Build extended-host timeout from 30 to 60 minutes after a reproduced shared-runner cancellation interrupted the full-game localization gate despite every preceding host gate passing.
- Serialize concurrent producers of the shared compressed battle-animation object so parallel modern profile links cannot tear data_banim.o or its symbol sidecar.
- Fix a fresh, independent verifier-reproduced defect in the read-only release/publication rehearsal: `check`/`summary`/`rehearse` now route through one single, shared top-level exception boundary so a well-formed-but-nonexistent --target-sha (in a real git repository) and the documented non-git/extracted-candidate path (with or without its required exact 40-lowercase-hex --target-sha override) never traceback as an unhandled exception (which collided with EXIT_NOT_ELIGIBLE); both now fail actionably as EXIT_TOOLING_ERROR (2), while a well-formed extracted candidate genuinely produces canonical BLOCKED JSON end-to-end. `evaluate_rebuild_eligibility()` never invokes `git submodule status` (or any other git command) against a non-git repo-root, and a declared allowlist member with no on-disk representation at all is now a controlled, actionable refusal instead of a silent omission. Publication remains mechanically BLOCKED throughout; no new capability or eligibility change. (#9)
- Fix a guardian-correction-review evidence defect in the read-only release/publication rehearsal: release_process.md and the closure-candidate report previously claimed a test named test_hermetic_eligible_rebuild_runs_twice_and_verifies_success drove rebuild_rehearsal_blocker() itself end-to-end to verified_success; it actually only ever called the legacy, copy-based run_build_twice() helper directly (never wired into the release status at all) and asserted nothing about the manifest-facing status. run_build_twice() is deleted outright. RebuildRehearsalBlockerEndToEndBuildTests now drives rebuild_rehearsal_blocker() itself, through a synthetic approved+pinned+initialized submodule fixture, through every one of verified_success/a mismatch/a build failure/a missing declared output/a shared-directory refusal/a source mutation, asserting status, distinct materialization roots, unchanged inputs, and matching declared outputs. Publication remains mechanically BLOCKED throughout; no new capability or eligibility change. (#9)
- Fix a source comment in archive_rehearsal.py that named a test class, RebuildRehearsalBlockerEndToEndVerifiedSuccessTests, which has never existed under that name; corrected to reference the real, existing RebuildRehearsalBlockerEndToEndBuildTests a fresh, independent re-review found. Adds a mechanical SourceCommentTestClassReferenceTests check scanning every scripts/release_rehearsal/*.py source file for a backtick-quoted test-class-name reference and cross-checking it against the real test classes actually defined under scripts/release_rehearsal/tests/, so a future stale reference of this exact kind fails automatically instead of silently drifting. Publication remains mechanically BLOCKED throughout. (#9)
- Rename the generated allowlist/export-exclusions documents' misleadingly-named, never-validated 'generated_from_sha' field to 'generation_basis_sha' and document explicitly, in both generators' own '_comment' text, that this field is purely a documentary record of which commit a document was last regenerated against -- it is never read, compared, or otherwise cross-checked by any validator in this repository (every check always independently re-derives its own live target_sha), so it must never be mistaken for a validated commit binding. (#9)
- Classify committed FE8J CP932 provenance payloads with the typed `.cp932` extension instead of prohibited generic `.bin` names, reject future raw-provider manifests that restore the old artifact class, add a public final localization gate that reports the verified authorized-ROM hash and exact slices, and isolate modern localization/debug lifecycle changes from the archival agbcc lane. (#18)
- Fix modern UI and memory layouts so inline Language selection, player attacks, battle animation, and resume remain valid. (#19)

### Security

- Hash-lock Build and release Python dependencies before artifact publication.
- Make modernization bitfield scanning linear-time for adversarial source declarations.
- Add a three-way immutable binding validator for the mgfembp git submodule (.gitmodules section, HEAD tree gitlink, export-exclusion record, and provenance record must all agree exactly on path/URL/pinned commit); reject a non-https submodule URL scheme and any allowlist/exclusion contradiction. Never fetches or initializes the submodule. (#9)
- Add explicit, factual export exclusions (docs/release_data/export_exclusions.json) and prove the included source allowlist and export exclusions are an exact, disjoint partition of the complete immutable HEAD tree; wire exact archive-member equality and non-git closed-world coverage into the rehearsal. (#9)
- Add external/human attestation as a permanent, non-bypassable overall-status dimension (check_external_attestation always reports missing, folded into build_manifest -- proven even with every other sub-check mocked to pass); document workflow-guard/action-pins as advisory, never authorization; and rebuild the verified-success path on two independently-materialized immutable git-archive source trees (never a copy of the live worktree), verifying neither materialization is mutated during the build. (#9)
- Bind every included source-release allowlist path's exact Git mode (100644/100755/120000) in docs/release_data/source_allowlist.json (schema_version 4's new 'modes' map), cross-checked for an exact bijection and, for a real git repository, against the live tree (check_mode_identity) -- a committed executable-bit/mode change now makes this canonical data stale/fail until regenerated, instead of being invisible to the allowlist. The archive itself continues to canonicalize every written member's tar mode to a fixed 0o644 regardless of the source Git mode -- a deliberate, now-documented and now-tested determinism policy (see 'Archive member mode policy' in docs/release_process.md), not an accidental omission. Publication remains mechanically BLOCKED throughout. (#9)
- Bind every included source-release provenance record to its exact Git blob OID and a deterministic SHA-256 of the raw blob content; a changed or new blob now invalidates its old provenance record (missing/stale) instead of silently passing on path match alone, and the deterministic generator never carries over approval/reviewer facts across a blob change. (#9)
- Close a closed-world enumeration gap a guardian-correction review found: the non-git candidate-tree enumeration tree_coverage.py/allowlist.py used for a genuine extracted-archive candidate (_present_regular_files) previously skipped straight past any symlink it found, making a stray, unlisted symlink at any path completely invisible to both the 'extra'/'missing from allowlist' accounting -- the replacement (_present_paths) never skips a filesystem entry by kind (regular file, symlink, hardlink, device, FIFO, socket); only a genuine, non-symlink directory is still ever walked through rather than reported. Also adds a dedicated regression test forcing build_deterministic_archive's own wired archive-member-exact refusal (not merely the tree_coverage.check_archive_membership_exact helper in isolation) through the real call site. Publication remains mechanically BLOCKED throughout. (#9)
- Close a mode-binding schema-downgrade gap a fresh, independent re-review found: load_allowlist_modes() previously returned None with no error at all when source_allowlist.json's 'modes' key was simply absent, and nothing anywhere checked schema_version itself -- deleting 'modes', or rolling schema_version back, silently disabled every one of the D4 mode-binding checks with no actionable failure while every other allowlist check kept passing. load_allowlist_modes() now hard-requires schema_version to be exactly the current, single supported value (4) before any mode checking is even attempted, and 'modes' itself becomes unconditionally mandatory once schema_version passes -- a missing, downgraded, upgraded-but-unknown, or wrong-type schema_version, or a missing 'modes' key, is now always an actionable AllowlistError. Publication remains mechanically BLOCKED throughout. (#9)
- Close a provenance blob-identity gap a guardian-correction review found: check_blob_identity() previously exempted all three provenance-manifest files (code.json, assets.json, submodules.json) from live-content cross-checking, even though only code.json's own self-record is structurally self-referential (assets.json/submodules.json's own records live inside code.json, a different file, so cross-checking them has no cycle at all) -- committed tampering of either file could silently evade identity validation. Fixed structurally: docs/release_data/provenance/code.json is now its own exact, minimal, documented export exclusion (tree_coverage.KIND_SELF_REFERENTIAL_EVIDENCE) rather than an included allowlist member, so it never requires or receives its own provenance record; the old SELF_REFERENTIAL_PROVENANCE_PATHS exemption is deleted entirely -- no included blob is exempt from OID/SHA256 validation any more. Exact tree coverage, the allowlist generator, and the provenance generator/coverage-required-path computation are all generalized to the second exclusion kind. Committed tamper probes cover assets.json, submodules.json, a changed same-path blob, and the excluded self-referential-evidence path itself. Publication remains mechanically BLOCKED throughout. (#9)
- Close a self-referential-evidence export-exclusion injection gap a fresh, independent re-review found: any tracked path could previously be claimed under tree_coverage.KIND_SELF_REFERENTIAL_EVIDENCE with a fabricated oid and tree coverage stayed clean, silently moving an arbitrary blob out of the archive+provenance-required set with no review. This kind's path is now checked against a small, hard-coded, human-curated policy set (SELF_REFERENTIAL_EVIDENCE_PATHS, today exactly docs/release_data/provenance/code.json) as a validator invariant enforced in both load_exclusions() and independently again inside check_partition() itself, so even a directly-constructed exclusion entry that never went through the loader is still caught -- no prefix, no wildcard, no extra row of this kind for any other path. Publication remains mechanically BLOCKED throughout. (#9)
- Close a submodule-content integrity gap a guardian-correction review found: a dirty-but-commit-matching mgfembp submodule worktree (locally modified/staged/untracked bytes, even though its own HEAD commit still matched the provenance-pinned commit) could previously be copied live (shutil.copytree) into both independent rebuild materializations and reported verified_success over tampered content. evaluate_rebuild_eligibility() now additionally requires a genuinely clean submodule worktree/index (git status --porcelain, run inside the submodule itself), the submodule's own configured remote URL to agree with .gitmodules's declared URL, and the pinned commit to be a real, locally-accessible object inside the submodule's own object database. Independently, the double-build's submodule-content materializer (_materialize_verified_submodule_content(), replacing _copy_verified_submodule_content()) no longer reads the submodule's live worktree at all -- it extracts the pinned commit's own immutable tree via a second, independent 'git archive <pinned_commit>' run inside the submodule's own repository, exactly mirroring how the superproject's own content is materialized. Publication remains mechanically BLOCKED throughout; the real mgfembp submodule remains uninitialized/unapproved and is never fetched/initialized/approved by this change. (#9)
- Close an allowlist/tree_coverage exclusion-consumer asymmetry a fresh independent review found: allowlist.py's own _load_non_gitlink_exclusion_paths() reader accepted any exclusion entry whose kind merely was not the literal string 'gitlink', with no curated-path or oid-shape check at all -- so al.check()'s own sub-report could stay clean for an arbitrary/uncurated self-evidence exclusion, or one with a fabricated oid, even though tree_coverage.check_partition() reading the exact same file already rejected it. This reader now delegates entirely to tree_coverage.load_exclusion_paths(), restricted to KIND_SELF_REFERENTIAL_EVIDENCE, so the same curated SELF_REFERENTIAL_EVIDENCE_PATHS policy set and oid-is-null invariant are enforced identically on both consumers. New literal reviewer-reproducer tests cover an arbitrary path with a fabricated oid, a curated path with a fabricated oid, and the full al.check() end-to-end path. (#9)
- Close submodule rebuild-eligibility fail-open gaps a fresh, independent re-review found: evaluate_rebuild_eligibility()'s prior URL check only ever compared the submodule's configured remote.origin.url against .gitmodules's declared URL when both sides already happened to be known, leaving a genuinely missing origin vacuously unexercised instead of ineligible. Eligibility now requires a live, non-empty configured origin agreeing exactly with both .gitmodules's declared URL and the separate docs/release_data/provenance/submodules.json record's own URL, defaulting to non-eligible; a missing declaration in either immutable source, or a mismatch against either, is its own actionable finding. Every underlying git status/git config/git cat-file command is now itself required to actually succeed -- a genuine tooling failure (distinct from the command's own ordinary not-clean/no-origin/object-absent outcome) is caught and reported rather than silently swallowed into a false pass. Publication remains mechanically BLOCKED throughout; the real mgfembp submodule remains uninitialized/unapproved. (#9)
- Remove false OID semantics from the self-referential-evidence export exclusion (docs/release_data/provenance/code.json) a fresh, independent re-review found: its oid field was never actually cross-checked against anything while nearby prose read as if it were an immutable, verified fact, and the checked-in value had already drifted stale. There is no truthful, immutable oid for this kind -- a file cannot record an exact hash of its own not-yet-finalized content without an unsolvable cycle. The schema now requires this kind's oid to be absent or JSON null; load_exclusions() hard-rejects any supplied/stale/fake value and generate_exclusions_document() always writes null. A gitlink exclusion (mgfembp) is unaffected and still requires and strictly cross-checks its own exact, immutable oid. Every included blob remains exactly OID/SHA256-bound with no exemption at all; code.json is documented explicitly as a curated path-only-plus-mode exclusion -- external rehearsal evidence about this repository's own tooling, never source archive content or a redistribution/legal authorization. Publication remains mechanically BLOCKED throughout. (#9)
- Add an authenticated one-shot Git authority publication broker with sealed plan-bound capabilities, separate host principals, write preflight, native pre-exec deadlines, typed signed outcomes, protected credentials and Git state, durable replay protection, and exact indeterminate-push reconciliation. (#205)

### Docs

- Close four factual-disclosure gaps a fresh, independent integration review found: document that history is never rewritten/amended/rebased, so an intermediate integration commit that is not itself individually green is expected and the branch tip (not every ancestor) is the verified integration unit; document that the immutable Git-blob-bound archive mode and a genuine git-archive-extracted non-git tree are two distinct, never-interchangeable hash domains (currently, exactly 510 tracked .pal palette files carry a .gitattributes 'text eol=crlf' export/checkout transform, so their committed blob bytes (LF) and their extracted/checked-out bytes (CRLF) differ, and the two archive modes' hashes must never be compared as the same artifact); clarify that CANONICAL_DIR_MODE (0o755) is a defined-but-unused constant -- every written tar member (only ever a regular file; no directory members are ever added) is unconditionally 0o644, never 0o755, so 'modes 0644/0755' must never be read as describing the archive's own output; and correct texts/expansion's provenance note, which previously, inaccurately inherited the general 'texts' root's original-game-asset/Nintendo-copyright note by directory-prefix seeding even though texts/expansion is repository-authored issue #18 localization-framework catalog/source, never extracted or derived from the original game -- PROVENANCE_ROOT_SEED now seeds texts/textdefs.txt, texts/texts.txt, and texts/expansion as three disjoint, non-overlapping roots, and docs/release_data/provenance/assets.json is regenerated; author/rightsholder/license/redistribution-approval remain exactly as unresolved (NOASSERTION/false/null) as every other tracked path -- nothing is invented. Publication remains mechanically BLOCKED throughout. (#9)
- Correct docs/release_closure_candidate.md's R5 section, which a fresh independent review found mislabeled the pre-existing, already-committed chmod-detection test (test_committed_executable_bit_change_makes_mode_data_stale, added in the guardian-correction remediation round's D4 fix) as one of R5's 'new tests'. The wording now states plainly it is not new to this round and was only re-verified end-to-end here. (#9)
- Correct release_process.md and the closure-candidate report to name the actual wired rebuild-verified-success function/test (never the deleted legacy run_build_twice()); document the two export-exclusion kinds (gitlink and self-referential-evidence) and the new mode-binding/'Archive member mode policy'; tighten the external-attestation section to state explicitly that Git-blob immutability binds committed bytes, never protects against what a candidate author chooses to commit, and that no candidate-controlled config/data/flag/env input can ever satisfy it; and correct the provenance generator's own documentation to state that a changed/new blob resets any previously-recorded approval/reviewer/legal fact (nothing is ever 'preserved') and that a submodule's pinned commit/URL are always re-read from the immutable target tree/.gitmodules. Publication remains mechanically BLOCKED throughout. (#9)
- Document the R1-R5 remediation round (self-referential-evidence validator invariant and null-OID semantics, submodule origin/command fail-closed eligibility, a corrected source-comment test-class reference, and unswitchable mode-binding schema enforcement) in docs/release_process.md and docs/release_closure_candidate.md; the candidate remains mechanically BLOCKED. (#9)
- Document the full policy-guardian mandatory-correction round (immutable Action pins, exact tree coverage/export exclusions, per-blob provenance identity, mgfembp three-way binding, external attestation outside candidate control, advisory workflow guard, and independent immutable rebuild materialization) in docs/release_process.md and docs/release_closure_candidate.md; the candidate remains mechanically BLOCKED. (#9)
- Fix a stale source comment in manifest.py's check_provenance() that still described the self-referential-evidence export-exclusion record as carrying 'kind/mode/oid/reason' -- this kind's oid field was already fixed (R2) to always be JSON null and never a claimed/cross-checked identity fact; the comment now says so explicitly instead of implying an oid is part of its evidence. (#9)
- Make export_exclusions.json's generated '_comment' kind-specific and truthful, per a fresh independent review: it previously claimed every exclusion entry has 'immutable mode/OID (as recorded by git ls-tree)', which is false for kind self_referential_evidence (whose oid is always JSON null and is never cross-checked). The comment now states each kind's actual, distinct semantics explicitly (gitlink: mandatory, strictly cross-checked oid; self_referential_evidence: exact path+mode+reason only, oid always null, never claimed or cross-checked). (#9)

### Internal

- Add a read-only release/publication rehearsal system (policy, changelog fragments, release manifest, migration registry, source-release guard/provenance, deterministic archive rehearsal, and a read-only CI workflow); publication remains mechanically BLOCKED pending human license/provenance approval. (#9)
- Close a latent identity-confusion defect found in fresh review: a genuine non-git extracted release candidate nested inside an unrelated outer Git repository could have scripts/modernize/expansion_config.py's resolve_build_commit() silently adopt that outer repository's HEAD (via git's own upward directory discovery) as this candidate's internal build-commit identity, even though that field is not currently published. resolve_build_commit() now only ever invokes git when repo_root is itself bound to its own .git metadata; the release manifest (check/summary/rehearse) now threads the already-validated, exact target SHA (an explicit --target-sha override in non-git/archive mode, or the real repository's own resolved HEAD) into the embedded build identity as its single source of truth, so a nested non-git candidate never makes any git subprocess call and never substitutes the unresolved 'unknown' sentinel where the exact override is required. Publication remains mechanically BLOCKED throughout. (#9)
- Close residual gaps a fresh, independent verifier reproduced against the read-only release/publication rehearsal system after all ten original findings were already closed: the workflow guard now rejects bare `nc` invocations (not only flagged ones), npm/yarn/pnpm publish and docker push/login, and command-position shell-variable/fragment command assembly (e.g. `X=cur; Y=l; $X$Y`, `${X}${Y}`, and a single locally-assigned variable invoked directly as a command), without ever flagging this repository's own safe dynamic job-summary redirection or ordinary data interpolation; and the version-ledger consistency check, wired through the live manifest path, now rejects a `status: "current"` entry that itself carries a non-null EOL date, and a `previous_supported_version`/`next_supported_version` that is absent from the ledger's own `supported[]` array, not a unique entry there, or carrying a status incompatible with its topological role. Publication remains mechanically BLOCKED throughout. (#9)
- Close the final focused-review shell-indirection gaps in the read-only release/publication rehearsal system's workflow guard: legacy backtick command substitution (`` ` ... ` ``) is now recognized as a command position exactly like `$( ... )`, so a variable/fragment-assembled or previously-tracked-variable command hidden inside a backtick pair (instead of `$( ... )`) is rejected the same way; `read`/`read -r` now tracks every variable name a multi-variable `read A B` populates, not only the first; and shell process substitution (`<(...)`/`>(...)`, unused by the real workflow) is now conservatively rejected outright. Ordinary/prose backtick usage (including this real workflow's own markdown-style comment backticks), literal/escaped backticks, and safe `read`/data-only usage remain unflagged. Publication remains mechanically BLOCKED throughout. (#9)
- Close three further residual shell-indirection gaps a fresh, independent verifier reproduced against the read-only release/publication rehearsal system's workflow guard: a variable/fragment-assembled command executed inside a `$( ... )` command substitution (e.g. `echo $($X$Y ...)`, `echo $(${X}${Y} ...)`, and a direct `echo $($CMD ...)` where `CMD` was locally assigned elsewhere), a variable assigned via `export NAME=value` and later invoked directly as a command, and a variable populated via `read`/`read -r NAME` and later invoked directly as a command -- all without ever flagging this repository's own safe dynamic job-summary redirection, an ordinary literal `$(date)`-style command substitution, or ordinary `export`/`read` usage that is only ever read back as data. Publication remains mechanically BLOCKED throughout. (#9)
- Close two critical findings from a fresh, independent final review of the read-only release/publication rehearsal system: the workflow/action-pin `uses:` scanner no longer mis-treats an unquoted `#` glued directly onto a preceding plain-scalar character (e.g. `- {name: setup#, uses: evilcorp/upload-secrets@main}`) as a YAML comment (a fail-open bypass that discarded the real `uses:` key and everything after it on the same line), now recognizing `#` as a comment only when it is the first character on a line or is preceded by whitespace, exactly like real YAML; and `source_allowlist.json`/`export_exclusions.json`'s own `generation_basis_sha` field, which both documents' schema/comments promise names a commit, is now mechanically checked (and both CLIs' `--write` now refuses an `--target-sha index`-derived tree basis outright) so a dangling, unreachable tree object can never again be silently recorded as if it were a commit. Publication remains mechanically BLOCKED throughout. (#9)
- Close two residual fail-open gaps found by a combined self-review of the read-only release/publication rehearsal system's four latest hardening slices: the structural, prefix-tolerant ZIP detector (`source_guard.classify_zip_structure`) is now wired into the dominant, real-git-repo archive path (`archive_rehearsal._hard_deny_check_git_entry`), so a tracked git blob whose committed content is a prefixed/self-extracting ZIP is rejected exactly like the filesystem and tar-member paths already reject it; and the workflow/action-pin `uses:` scanner no longer silently skips an undecodable (malformed-escape) quoted key sitting in genuine key position, which previously let a completely unpinned/mutable action reference (e.g. bare `checkout@main`) sail through unflagged whenever the key in front of it carried one bad escape byte -- it is now always surfaced as its own hard, fail-closed violation. Also corrects `docs/migration_registry.md`, which still described a single conflated "epoch" model, to accurately document the exact `(format_version, compat_epoch)` source/target pair model and the underlying tool's `--to-format-version`/`--to-compat-epoch` vs. `--to-epoch` semantics. Publication remains mechanically BLOCKED throughout. (#9)
- Remediate a fresh, independent code review of the read-only release/publication rehearsal system: provenance coverage is now a literal, one-exact-record-per-allowlisted-member bijection (no directory-prefix/category inheritance, plus a submodule gitlink-pin cross-check) with a deterministic generator/checker; source_guard/archive_rehearsal candidate-membership checks (filesystem closed-world scan, archive members, and the non-git archive-build fallback) are exact-path-only, never directory-prefix; the workflow guard now rejects any permission scope granted write (not merely contents), including future-unknown scopes; and release.mk/release docs now correctly state that GNU Make collapses any failed recipe's exit code to 2, distinct from the underlying CLI's own 0/1/2/3 contract. Publication remains mechanically BLOCKED throughout. (#9)
- Remediate all ten independent-verifier findings against the read-only release/publication rehearsal system: exact per-member source allowlist and provenance coverage, expanded hard-deny rules with an audited map/hex exception list, immutable Git-blob-bound archive rehearsal, a truthful four-state rebuild rehearsal, additional manifest consistency validators, a machine-distinct exit/status contract, a dynamically-rendered CI job summary, a hardened workflow guard, and fixed broken docs links; publication remains mechanically BLOCKED throughout. (#9)
<!-- release-notes:unreleased:end -->

## [0.1.0] - baseline

The framework's semantic version and GBA ROM identity fields were
established in `config.mk` (issue #8) before this changelog/fragment
system existed; no fragment history is recorded for it. See
`docs/config_identity.md`.
