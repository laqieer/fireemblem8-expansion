/* Terminator entry has the wrong shape (two fields instead of one, and
 * not -1). */
static CONST_DATA struct WeaponTriangleRule sWeaponTriangleRules[] = {
    { ITYPE_SWORD, ITYPE_LANCE, -15, -1 },

    { 0 },
};
