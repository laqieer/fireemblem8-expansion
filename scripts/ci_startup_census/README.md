# Disposable ordinary-CI startup census 1

**Never merge this branch. Preparation is not launch authorization.** Main
must independently review the exact committed harness and freeze its separate
first-creation launch before any push. This is not a production fix, root19,
provider/H1 task, calibration, graph report, or retry of a closed witness.

## Scope and history

- Delivered base: `ec1dc8553419c8833a687fd8d4a6521a4e29ff7a`.
- Separately checked-out, unchanged candidate:
  `048c1bb3ab8008bbe862ad8072ed124e02fdb170`.
- Preparation contract:
  [issue #180, comment 5725928837](https://github.com/laqieer/fireemblem8-expansion/issues/180#issuecomment-5725928837).
- Historical Build `35307822585`, native job `105483623413`: seven subcases
  in four null-mount methods failed at the common first outer `uid_map` write,
  before the production helper. Source and complete stderr distinguish this
  from the later inner launch. The caller-denial and original setup controls
  passed. A separate control recorded real UID/GID1001, a genuine ordinary
  namespace-preflight failure and the established privileged route's success.
  Its peer UID0 describes supervisors, not the test driver.

The failing mapper's effective IDs, maps, capabilities, NNP, dumpability and
LSM decision were not captured. Do not transplant the earlier local parent0
mapping explanation, infer AppArmor, or call this a selective-helper failure.
Root18 semantic attribution and every existing failed diagnostic remain
separate and unchanged.

## Closed execution

The new workflow admits only the public repository owner's first creation
push of `diagnostic/issue-180-null-startup-census-1`, workflow run1/attempt1.
Its program independently checks raw event fields and workflow/source identity.
The harness may add only this directory and this one workflow relative to BASE.
Both checkouts are exact and clean; credentials are not persisted. Permissions
are read-only contents. There are no secrets, publisher, dispatch trigger,
reroute, mapping helper, automatic retry, or merge path.

The runner label, 60-minute setup-job envelope, recursive candidate checkout
and dependency-install command match the declared native job. Only that
existing package-install command uses sudo. The attempt runs as the ordinary
user from the candidate cwd. It uses the selected candidate's literal
`authority.ENVIRONMENT` and unchanged `lifecycle.py`, absolute executables,
original `--user --map-current-user --keep-caps --mount --fork --kill-child
--propagation private` ordering, and original payload-argument shape.
The fixed mode name is `unsupported`, the first failed native subcase;
no mode-specific operation actually executes.

An exclusive scope and attempt record prevent reuse. Setup failure consumes
the prospective allocation. Prelaunch facts are bounded and allowlisted:
real/effective/saved IDs, groups, selected status/capability/NNP/seccomp fields,
maps and their ownership, namespace identities/owners, read-only dumpability,
LSM label, kernel/Python/util-linux versions and narrowly listed readable
restrictions. Unreadable facts retain their errno/unavailable status.
No environment, secret, whole-proc or source-content dump is uploaded.

The payload emits an authenticated first-outer-entry marker and one census,
then exits. It never creates the original tmpfs, binds devices, changes
capabilities/NNP, launches an inner namespace, performs null I/O, or calls a
production helper. The original unshare mount namespace and private propagation
are part of the unchanged launcher prefix, not fixture mounts.

The independent watchdog is30 seconds; coordinator wait is35; the outer command
has a hard45-second bound. Each raw stream is at most256KiB. The owned fixture
contains only the bounded harness copy and authority record (below1MiB).
Lifetime pipes, pidfds and the original watchdog retain process ownership.
The existing lifecycle signal-deferral interface closes the parent acquisition
window; the child restores the original inherited mask before its original
file-size limit. The exact argv/deadline is persisted before launch, including
when a later hard kill prevents a final result.
Cleanup failures remain failures and preserve uncertainty; they cannot overwrite
the first mapping error. A hard outer kill or setup failure can leave no final
result record: that is incomplete evidence, never success.

## Context differences and interpretation

This is a fresh job and fresh interpreter, not the historical image, kernel,
LSM state or process after the preceding native tests. It adds read-only
observations before launch and places the owned fixture in runner temp rather
than the candidate tree. The actual kernel/util-linux versions are recorded;
the same `ubuntu-latest` label does not prove image equality.

Mapping failure leaves post-mapping child facts explicitly unavailable; parent
facts are never copied into that field. Cold success means only that this
fresh startup reached the first entry. It does not repair or qualify the
historical fixture, any of its seven helper behaviors, or a root report.
No permitted alternative topology or privileged fixture route is inferred.
Any future fixture repair needs its own freeze and original positive,
adversarial, restoration and cleanup controls.

## Benign preparation controls

From this non-delivery source tree, with the pinned candidate Git object
available locally:

```sh
python3 -B -m unittest scripts.ci_startup_census.test_census -v
actionlint .github/workflows/issue180-null-startup-census-1.yml
```

These controls parse the real workflows, source AST and event predicates,
exercise synthetic telemetry and mocked launch/cleanup APIs, and use only
owned temporary files. They do not run `plan`/`run` against a real hosted event,
collect local census facts, execute namespace commands, or import/run the
production test suite. All original four methods/seven expectations, production
source, existing Build workflow, permissions, skips and numeric limits remain
unchanged.
