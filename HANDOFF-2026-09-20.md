# Recovery handoff: issue #180 and Discussion #174

**Recovery checkpoint, not delivery.** The user requested preservation after
session `3df36741-371b-49d8-ab09-a70c1ff3bec0` aborted, then continuation in a
new session. The screenshot shows the CLI's Node.js process exhausting its
JavaScript heap. It is **not** another ownership-probe budget failure and
does not justify changing the repository's resource limits.

The original objective inherited from session
`d581c36c-d935-4b38-886d-2b3271a671ce` remains incomplete: finish accepted
issue #180 / PR #186, then the dependent issue #196, and reconcile
Discussion #174. Thirty-three independent roots have already been delivered.
No further user design decision is currently required.

## Start here

1. Read this document, the project `development-workflow` skill, and only the
   relevant files in [handoff-materials](handoff-materials/README.md).
2. Verify current GitHub heads before editing. The recovery refs below are
   backups, **not reviewed candidates, successful experiments, or merge
   instructions**. Do not merge this handoff branch into the implementation.
3. Resume the three bounded workstreams below. Header/GCC review findings
   remain open; queued agent work did not become completed work when the CLI
   crashed. Prior agent handles are unavailable in the recovered runtime.
4. Keep native execution separate from pure/inert preparation. Do not push
   an original diagnostic/calibration branch or run a diagnostic to discover
   whether it works. Such actions consume separately frozen allocations.

All backup branches use the prefix:

```text
recovery/session-3df36741-20260920-
```

The main handoff lives on the branch ending in `handoff`, at this file path.
For example, read it without changing an implementation checkout:

```sh
git fetch origin
git show origin/recovery/session-3df36741-20260920-handoff:HANDOFF-2026-09-20.md
```

## Exact current and recovery revisions

| Purpose | Branch / ref | Exact commit |
| --- | --- | --- |
| Delivered default branch | `master` | `ec1dc8553419c8833a687fd8d4a6521a4e29ff7a` |
| Published PR #186 | `agent/issue-180` | `c8b365da1be29bc58352cf1edb8b836a2cf18321` |
| Unreviewed header implementation backup | recovery prefix + `header` | `453797b8d74b6da2ce96bb812dd0f0bbfeaa1c98` |
| Dormant GCC protocol backup | recovery prefix + `toolchain` | `bfc9a4ba53c369db5ac4a6834ee14830e1eaae78` |
| Interrupted bootstrap amendment backup | recovery prefix + `bootstrap` | `3302f790e944e81be4ea0777682f5282e560fd9c` |

The header and GCC commits are separate normal children of `c8b365d`.
The bootstrap chain is:

```text
3302f790e944e81be4ea0777682f5282e560fd9c  recovery WIP, unfinished amendment
20478394860b673b98fb32a4dd292fa0fc02a5d4  initial intentionally held preparation
ec1dc8553419c8833a687fd8d4a6521a4e29ff7a  delivered BASE
```

The bootstrap independently selects immutable source `c8b365d`; it is not a
production child of that source. It must never be merged into `master`.

The user explicitly requested pushing unfinished work for recovery. That
changes the earlier local-only preservation boundary **only for these backup
refs**, not for delivery, approval, native execution or diagnostic allocation.
The recovery names match neither Build CI's `master` push filter nor the
diagnostics' exact branch filters. No PR was opened for these backups.
The existing PR and `master` were not advanced.

## What survived the crash

Recovery inspected 325 registered worktrees, including all 96 belonging to
this session. Only one session worktree contained uncommitted source changes:
the bootstrap amendment's workflow, implementation and test file. Those exact
changes were committed as `3302f790e`, with the Copilot trailers.

The header worktree was still clean at `453797b8d`; **none of the five requested
review corrections survived as source edits**. The GCC worktree was still
clean at `bfc9a4ba5`; **neither requested protocol correction survived as source
edits**. Do not interpret their old `in_progress` todos as a later commit.
No correction handoff or completed correction result was available.

The bootstrap WIP adds 1,248 and deletes 114 lines across three existing
diagnostic files. Its complete BASE comparison is five additive files and
4,112 lines. Recovery checked Python parsing and `git diff --check` only.
It did not run the partially edited tests or any namespace/native workload.
The README still describes the earlier unconditional hold and must be
reconciled before this diagnostic can be considered ready.

Original helper/source/custody fixes through `c8b365d` remain accepted.
Historical adapter `b7bc6f58` and source-context `61856581` are already ancestors
of `c8b365d`; do not integrate them again.

## 1. Header authentication and shared native-return custody

**Local source:** `453797b8d74b6da2ce96bb812dd0f0bbfeaa1c98`.
Eight files, +1,401/-76 against `c8b365d`. Full PR comparison:
122 files, +62,056/-694 = 62,750 changed lines. Exact unchanged-file
mode/type/blob carry counts were A23/B33/C58; these are preservation evidence,
not behavioral acceptance.

The implementation authenticates the actual sequence of header-kernel
observations using a per-launch HMAC. It intentionally allows legitimate
conditional short transcripts and repeated operation/path pairs. It must not
require all five allowlisted paths or predict libselinux control flow.
One `ProbeSession`-owned issue/claim/retire boundary serves header results and,
after later integration, GCC results. Do not add a second native-return registry.

Read:

- [Original local scope](handoff-materials/issue180-header-receipt-implementation-scope.txt)
- [Shared implementation contract](handoff-materials/issue180-shared-receipt-freeze/contract.txt)
- [Exact fingerprint format](handoff-materials/issue180-toolchain-intermediate-receipt-design-v2/fingerprint-contract.txt)
- [Independent code findings](handoff-materials/issue180-header-receipt-implementation-evidence/independent-review.json)
- [Required correction scope](handoff-materials/issue180-header-receipt-correction-scope.txt)

Independent reviewer `195e0ba1` found five **Medium, high-confidence** defects:

| ID | Finding | Required correction |
| --- | --- | --- |
| HR1 | Full-width pending traversal tuples allocate before adequate admission. | Ancestor/index frames that schedule one child at a time; separately admit dictionary key references and sorting workspace. |
| HR2 | Row/terminal bound calculations and payload slices allocate before admission. | Admit calculation, escaping and copy/decode workspace before those operations. |
| HR3 | A failed reservation-to-observation transfer refunds its count. | Retain the spent marker through every fallible publication step; failures may overcount but never refund. |
| HR4 | Equal-byte replacement of an issued stdout/stderr field passes claim. | Require the original immutable byte references as well as existing type/length/digest/context checks. |
| HR5 | Failed/nonzero filter paths retain the key-bearing launch until after report publication. | Release the owned verifier before failed-path report construction, preserving successful sign-before-release. |

Author evidence: 11 inert tests passed in 0.023s, with a separate one-test
restoration control in 0.001s. Reviewer evidence: 11 passed in 0.027s but did
not cover the five faults. Original missing-import errors are preserved.
Exact commands/output were retrospectively transcribed; standalone original
logs and some tool-version/timing details were not retained. Do not invent them.
See the copied [handoff](handoff-materials/issue180-header-receipt-implementation-evidence/handoff.json)
and [summary](handoff-materials/issue180-header-receipt-implementation-evidence/summary.txt).

Correct these defects in a normal child, using actual pure/inert API preimages,
allocation-between-admission checks, fault injection and targeted restoration.
Keep budget/lifecycle, source interpretation, SDK profiles, namespace code and
shipping limits unchanged. The prior independent review must be followed up
against the exact corrected commit before a native qualifier is frozen.

The code-only request to add
`ArmHeaderPipelineTests.test_actual_kernel_completion_rejects_repaired_forgery`
also did **not** survive as a source change. Its
[scope](handoff-materials/issue180-header-native-case-preparation-scope.txt)
requires duplicating an actual row, repairing the public count/manifest and
necessary accounting, preserving the actual tag, and proving refusal before
filter result/output/cache/semantic publication. Earlier legitimate pipeline
effects are not falsely claimed absent.

The prospective smallest qualifier is the existing positive
`test_actual_arm_sdk_and_sed_keep_five_original_dispatches` followed by that
new negative, stop on first failure. The positive contains one ordinary Make
and one confined Make; the negative contains one confined Make. Thus the
proposal is **two selectors, three Make calls, two fixture/session lifetimes**,
not one native call. **Nothing is allocated yet.** Do not substitute the
existing four-negative method or run the full native-bearing class.

## 2. Dormant GCC intermediate protocol, then real integration

**Local source:** `bfc9a4ba53c369db5ac4a6834ee14830e1eaae78`.
Exactly two files, +1,244/-0:
`toolchain_runtime.py` and `tests/test_toolchain_runtime.py`.
The helpers are dormant data parsing/projection, not native authority.
Existing version-1 launch routing, `records()`, Controller and native-bearing
test-class behavior are unchanged.

Read the [preparation scope](handoff-materials/issue180-toolchain-protocol-preparation-scope.txt),
[handoff](handoff-materials/issue180-toolchain-protocol-preparation-evidence/handoff.json),
[independent findings](handoff-materials/issue180-toolchain-protocol-preparation-evidence/independent-review.json)
and [correction scope](handoff-materials/issue180-toolchain-protocol-correction-scope.txt).

Independent reviewer `1fb62bc2` found two **Medium, high-confidence** defects:

| ID | Finding | Required correction |
| --- | --- | --- |
| P1 | Admission callbacks are optional; non-ASCII data is sliced before its actual representation size is admitted. | Mandatory `reserve`, complete exact-string/ASCII/wire checks before copying, then representation-aware pre-growth admission. |
| P2 | Empty executions, `workspace=None`, or integer argv fields escape as raw `IndexError`/`TypeError`. | Validate shapes before indexing/conversion/iteration and refuse with `MakeProbeError`, without broad catches or success defaults. |

Author 15 inert tests passed in 0.059s; reviewer 15 passed in 0.070s while
separate adversarial controls exposed the defects. Neither correction was
implemented before the crash. Preserve preimages, correct them, and obtain
exact follow-up. Use only `ToolchainProtocolDataTests`, not the native-bearing
module/class.

The eventual live implementation must follow the
[V1 design](handoff-materials/issue180-toolchain-intermediate-receipt-design/design.md)
as amended by [V2](handoff-materials/issue180-toolchain-intermediate-receipt-design-v2/amendment.txt)
and the shared header contract. **V1's two-FD claim and competing native-return
registry are superseded:** the mechanism's explicitly derived peak is three
FDs, and native-return ownership is shared in `ProbeSession`.

Still absent: actual creator/writer/reader/content/retirement syscall proof,
streaming stopped-memory capture, shared native-return purpose activation,
one-use step/recipe capabilities, immutable raw sidecars/occurrence archive,
source-context binding, issuer-gated semantic projection, coupled docs/cases,
and genuine qualification. Only the two grammar-derived temporary-file
operands may change semantic representation; real content and all other
arguments/input/tool/status/output/kernel facts remain meaningful.

Do not publish this dormant preparation as a complete implementation. Resume
its integration only after the header shared seam is corrected and coordinated.
Use normal Git integration, not cherry-picking/rebasing/amending prior history.

## 3. Restricted-CI namespace fixture bootstrap

**Recovery source:** `3302f790e944e81be4ea0777682f5282e560fd9c`.
This is an unfinished, unqualified diagnostic, never a production merge.

The initial `204783` preparation correctly refused because R created a host
backing child whose original identity could not reach C through frozen c8
custody frames. Namespace destruction does not unlink that host directory.
That first commit had 36 benign tests, four actual-c8 serializer controls and
actionlint evidence; these results **do not cover** the recovered amendment.

The [amendment scope](handoff-materials/issue180-null-bootstrap-backing-correction-scope.txt)
chooses the smaller solution: mount the one private 1MiB tmpfs on C's original,
identity-bound fixture directory, then create R's `volume` child only inside
that filesystem. The pre-mount FD still points at the old host backing and
must be closed before creation through a separately verified mounted-root pin.
C already knows its own original host identities, so no frame/API/channel
expansion is needed.

The recovered edits attempt this placement and the formerly missing C
orchestration, plus inert tests and workflow artifact-path changes. They were
checkpointed exactly, not finished or reviewed during recovery.

**Resume precautions:**

- Reconcile the stale README, all new coordinator/failure paths and actual
  pure/inert test safety before running the smallest benign family.
- Preserve the original R/N/W role, owner-change, UID/GID-map, capability,
  NNP, FD, first-cause, old-operation and selective-helper checks.
- Keep immutable selected source `c8b365d`, one 30s original deadline, 35s
  wait ceiling, 45s enclosing ceiling, 1MiB fixture and existing capture limits.
- Do not manufacture cleanup identity from a late lookup, retry uncertain
  descriptors, recursively delete unproved paths or add privileged cleanup.
- The current code requires exactly two normal commits above BASE.
  `3302f790e` is already the second. A further normal correction needs an
  explicit exact-lineage contract/test update before launch; do not amend
  history or silently loosen the ancestry guard.
- The original `diagnostic/issue-180-null-bootstrap-1` branch remains absent
  remotely. **Do not push it now:** first creation would trigger the
  privileged diagnostic and consume its one-shot allocation.
- Independent exact-code review and a separate owner/exact-head/absent-branch
  launch freeze remain mandatory. Even representative success would not
  qualify all four Foundation methods/seven modes or integrate the backend.

See the [original role design](handoff-materials/issue180-startup1-concrete-bootstrap-design/design.txt),
[preparation scope](handoff-materials/issue180-null-bootstrap-preparation-scope.txt)
and [historical first handoff](handoff-materials/issue180-null-bootstrap-preparation-evidence/handoff.json).
The latter describes `204783`, not the recovered WIP.

## CI, experiments and remaining overall gates

At recovery, PR #186 is OPEN/MERGEABLE at `c8b365d`, not merge-ready.

| Evidence | Actual state |
| --- | --- |
| Master Build `35061973982` | Passed on delivered `ec1dc855`; prior remote completion passed. |
| Candidate Build `35391530857` | Failed: 684 native tests with the same seven UID-map fixture failures and one skip; 384 ownership tests with the existing aggregate control-cap error. |
| Candidate Copilot review `5252363578` | COMMENTED, 59/122 files, zero new inline comments; not full-diff acceptance. |
| Earlier review `5251759482` | Substantive suppressed header-receipt finding confirmed and still open. |
| Candidate CodeQL/security `35391528801` / `35391537638` | Passed. |
| Active workflow observers | None; all prior observers were consumed. |

Original roots 1-20, the ordinary startup census and the two-call provenance
experiment are spent/closed. No root21, second census, bootstrap run, new
header qualifier, provider/H1 run or full-resource measurement is allocated.
Prior four provider calibrations and the invalidated historical lease remain
spent; do not infer a fifth.

Root20 qualified seven containment controls and passed the preceding source
analysis, then refused changed command provenance. It retained no exact
expected/actual dynamic-command difference and produced no public report.
The subsequent two-call component experiment attempted two Make observations
but only the first returned. Its two actual checker records differed only in
the matching cc1 output/assembler input temporary paths. The second call
failed in SDK policy initialization; its exact failed row/launch bound was
unretained. Do not turn it into a completed pair or retry a closed allocation.

After the three current workstreams, still required:

1. Genuine original-root/source acceptance and deliberate public per-pass
   reporter adoption; the public source mode remains default/invariant.
2. Complete public-report and independent-verifier measurements, then a
   resource policy derived from complete evidence rather than guessed quotas.
3. Actual current foundation-introduction trusted review/admission (H1),
   independently selected immutable verifier and exact source/task/PR/base/
   head/check/result binding.
4. Exact candidate Build/Copilot/full independent coverage, autonomous merge,
   immediate open-PR conflict scan, exact-master consolidated Build/patch,
   issue evidence/closure and `make remote-completion-check`.
5. Independent issue #196 workflow extraction, any genuinely necessary
   separate prerequisite, Discussion #174 reconciliation and
   `make all-issues-completion-check`.

The user approved contained measurement and necessary indivisible PR-size
exceedance, not weakened ownership/security/source checks or unbounded shared
host work. A partial run is not a completed calibration. Local iteration stays
focused; one owner-context pusher/coordinator watches each exact run using an
attached shell, never a polling reasoning agent.

## Persistent public records

- [Canonical PR evidence](https://github.com/laqieer/fireemblem8-expansion/pull/186#issuecomment-5485313528)
- [Canonical Discussion evidence](https://github.com/laqieer/fireemblem8-expansion/discussions/174#discussioncomment-18206532)
- [Earlier full PR archive](https://github.com/laqieer/fireemblem8-expansion/issues/180#issuecomment-5733568636)
- [Earlier umbrella archive](https://github.com/laqieer/fireemblem8-expansion/issues/180#issuecomment-5733568642)
- [Indivisible review-size exception](https://github.com/laqieer/fireemblem8-expansion/issues/180#issuecomment-5649758306)
- [Header correction scope](https://github.com/laqieer/fireemblem8-expansion/issues/180#issuecomment-5737364905)
- [Coherent-forgery case preparation](https://github.com/laqieer/fireemblem8-expansion/issues/180#issuecomment-5737416238)
- [GCC protocol correction scope](https://github.com/laqieer/fireemblem8-expansion/issues/180#issuecomment-5737326387)
- [Bootstrap placement amendment](https://github.com/laqieer/fireemblem8-expansion/issues/180#issuecomment-5737056729)

Issue #196's read-only preparation remains local at
`F/issue196-readonly-preparation/`: 60 steps (56 Build + four drift),
nine jobs/34 leaf gates, 30 substantive embedded/control-flow programs.
No #196 implementation or extra prerequisite issue/PR has been created.
Preserve straight-line CLI/Make groups and the independently selected
same-repository full-SHA source boundary; candidate code cannot
self-authenticate.

## Local paths and evidence retention

`F` means:

```text
/home/laqieer/.copilot/session-state/3df36741-371b-49d8-ab09-a70c1ff3bec0/files
```

| Worktree | Local location |
| --- | --- |
| Published c8 source; keep stable for bootstrap | `/home/laqieer/.copilot/session-state/3b24c12d-8000-4372-8d56-2221d61ec1c6/files/issue-180` |
| Header implementation | `F/issue180-header-receipt-implementation` |
| GCC protocol | `F/issue180-toolchain-protocol-preparation` |
| Bootstrap | `F/issue180-null-bootstrap-preparation-worktree` |
| This handoff | `F/recovery-20260920-handoff` |
| Delivered master worktree | `/home/laqieer/.copilot/session-state/d581c36c-d935-4b38-886d-2b3271a671ce/files/master` |

Read only the named evidence needed. The session directory contains over a
million files; do not recursively list/archive/load it into the next session.
This handoff carries 22 explicitly selected design/scope/review records, not
raw native reports, environments, SDK/source captures, secrets, ROMs or crash
dumps. Copies are historical records with explicit supersession, not proof
that an old pending statement remains current.

Two raw Node.js crash reports remain untracked in the original checkout and
were not read or published. An older unrelated worktree at
`/home/laqieer/.copilot/session-state/662e6c0f-e2b3-4410-8478-62435c5873fd/files/head-baseline`
contains a pre-existing two-line forward declaration in `include/save_format.h`.
It is not this session's work and was left untouched. Other older sessions'
unpublished branches were likewise not adopted into this recovery.

No active shell survived. Messages to the three old implementation-agent IDs
returned `not found in visible scopes`; they are historical attribution only,
not live workers to poll.

## Additional historical session tips preserved

These eight local-only tips were also backed up, without execution or merging.
Use recovery prefix plus the suffix below. They are **historical reference
only**, not extra work to integrate or allocations to reactivate.

| Recovery suffix | Exact commit | Historical purpose |
| --- | --- | --- |
| `archive-3f8a1b51` | `3f8a1b51532182eb4d021e92422096768072399b` | Baseline9 complete-correction preparation |
| `archive-114a7470` | `114a747072532ed1f31edfd7b722c08bcec15cc7` | Baseline9 sealed defaults/mode preparation |
| `archive-c9b607cd` | `c9b607cdab718211ff272a385267f74f0cd08ca5` | Earlier bounded baseline9 preparation |
| `archive-6e626c73` | `6e626c73ba670b751b1239dfbc9cde023beaf608` | Baseline9 original-context preparation |
| `archive-0a767803` | `0a767803a3249ff82960d42d8e37e22e63ab2e8a` | Historical template-mode ordering |
| `archive-4fe46baf` | `4fe46baf44601682565de223106c2d582dbf265a` | Historical original-root composition merge |
| `archive-9fa82137` | `9fa82137b0bd04cc096a00df39fc45fcc280d232` | Historical root-context merge |
| `archive-167d08fb` | `167d08fb9126e9e5219bf2a4d37a0ee3ed575db8` | Historical toolchain case-layout merge |

## Suggested next-session instruction

> Continue issue #180 / PR #186 using HANDOFF-2026-09-20.md on
> recovery/session-3df36741-20260920-handoff. Recheck actual remote/local state
> first. Correct the five header and two dormant-GCC review findings, finish
> and review the recovered bootstrap amendment, then perform only separately
> frozen genuine qualifiers. Preserve independent workstreams, original
> security/source/resource boundaries and one-shot allocation history.
> Complete #180, then #196 and Discussion #174 through their remote gates.
> Do not treat recovery backups, stale todos or unavailable old agents as
> approval, completed implementation or runtime evidence.
